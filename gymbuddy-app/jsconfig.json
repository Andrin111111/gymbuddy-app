{
	"extends": "./.svelte-kit/tsconfig.json",
	"compilerOptions": {
		"allowJs": true,
		"checkJs": true,
		"moduleResolution": "bundler"
	}
	// Pfad-Aliase werden ueber https://svelte.dev/docs/kit/configuration#alias verwaltet
	// ausser $lib, das ueber https://svelte.dev/docs/kit/configuration#files verwaltet wird
	//
	// Wenn du includes/excludes ueberschreiben willst, kopiere die relevanten includes/excludes
	// aus der referenzierten tsconfig.json; TypeScript fuehrt sie nicht zusammen
}
